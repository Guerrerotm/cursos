<!DOCTYPE html>
<html>
  <head>
  <!-- Bootstrap CSS (ya lo tienes) -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM"
      crossorigin="anonymous">

<!-- Font Awesome para el ícono IA -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">

<!-- Summernote CSS -->
<link href="https://cdn.jsdelivr.net/npm/summernote@0.8.20/dist/summernote-lite.min.css" rel="stylesheet">

</head>
  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <!-- Logo -->
        <a class="navbar-brand" href="#">
          <%= image_tag("LOGO.jpeg", alt: "Universidad Tres Culturas", class: "img-fluid", style: "max-height: 50px;") %>
        </a>

        <!-- Botón hamburguesa en móvil -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContenido" aria-controls="navbarContenido" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Enlaces -->
        <div class="collapse navbar-collapse" id="navbarContenido">
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <li class="nav-item">
              
            </li>
            <li class="nav-item">
              <%= link_to "Cursos", cursos_path, class: "nav-link" %>
            </li>
            
          </ul>
        </div>
      </div>
    </nav>


    <div class="d-flex">
      <!-- Sidebar -->
      <div class="bg-light p-3" style="width: 250px; min-height: 100vh;">
        <div class="mb-4">
          <strong>MANUALES </strong> 
        </div>
        
        <h6 class="text-muted">CONTENIDO</h6>
        <ul class="list-unstyled mb-3">
          <%= link_to "📄 Generar curso", new_curso_path, class: "btn btn-primary btn-sm w-100 text-start mb-2 shadow-sm" %>

          <li>
            <button class="btn btn-primary btn-sm w-100 text-start mb-2 shadow-sm" type="button" data-bs-toggle="collapse" data-bs-target="#collapseCursos" aria-expanded="false" aria-controls="collapseCursos">
              📁 CURSOS
            </button>

            <div class="collapse" id="collapseCursos">
              <ul class="list-unstyled ms-2">
                <% if defined?(@cursos) && @cursos.present? %>
                  <% @cursos.each do |curso| %>
                    <li class="mb-2">
                      <%= link_to curso_path(curso), 
                          class: "d-flex align-items-center text-decoration-none text-dark fw-semibold curso-link" do %>
                        📄 <span class="ms-1"><%= curso.title %></span>
                      <% end %>
                    </li>
                  <% end %>
                <% else %>
                  <li class="text-muted">No hay cursos.</li>
                <% end %>
              </ul>
            </div>
          </li>
        </ul>

        <footer class="mt-4 text-muted small">
          &copy; 2025 Universidad <br>
          Tres Culturas UTC😄
        </footer>
      </div>

      <!-- Contenido principal -->
      <div class="flex-grow-1 p-4">
        <%= yield %>
      </div>
    </div>
<!-- jQuery -->
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>

<!-- Summernote JS -->
<script src="https://cdn.jsdelivr.net/npm/summernote@0.8.20/dist/summernote-lite.min.js"></script>

<!-- Bootstrap Bundle -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<!-- Rails Importmap -->
<%= javascript_importmap_tags %>

<!-- Script de Summernote con IA -->
<%= javascript_include_tag "summernote_ai" %>


</html>
